# Local Simplified-Chinese reading aids (see AGENTS.local.md); the English
# Markdown files are canonical and the zh-cn counterparts are optional.
*.zh-cn.md

# Dependencies
node_modules/

# Build output
web/dist/

# Local runtime state
# Codex auth is never copied into an image or committed; it is mounted read-only
# into a container at run time.
auth.json
codexhome/
*.db
*.db-journal
sprout.db
# The fallback contract file a working-directory engine (opencode) reads when
# the directory's own AGENTS.md belongs to the user. Sprout writes it at run time.
SPROUT-PROJECT-CONTRACT.md

# Editor and OS noise
.DS_Store
.idea/
.vscode/

# Local agent pipeline configuration
.agents/dev-pipeline
